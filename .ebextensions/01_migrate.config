container_commands:
  01_supabase_db_push:
    command: bash scripts/eb-migrate.sh
    # Runs on a single instance per deploy, and is skipped when autoscaling
    # launches new instances — the two properties a schema migration needs.
    leader_only: true
